import { Component } from '@angular/core';

// App component decorator.
@Component({
  selector: 'app-root', // Unique selector holds app route and is used as HTML directive.
  templateUrl: './app.component.html', // HTML template of component.
  styleUrls: ['./app.component.css'] // Styling of component
})

// Class.
export class AppComponent {

}
